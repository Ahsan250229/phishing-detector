# CI/CD Overview

This project uses GitHub Actions to automate:
- Code quality checks (lint)
- Unit tests
- Security checks (SAST / dependency scanning)

Notes:
- Workflows that execute in GitHub Actions must be placed in: `.github/workflows/`
- This folder (`ci-cd/`) is included to document CI/CD design for assessment evidence.
